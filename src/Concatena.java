// Questa classe contiene il metodo concatena
public class Concatena {
    // Metodo che accetta una stringa e un intero
    public static String concatena(String str, int num) {
        return str + num; // Concatena la stringa e l'intero
    }
}
